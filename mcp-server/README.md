# mcp-server

Inert placeholder. No MCP server exists yet — nothing in the app depends on this folder.

It mirrors the Claude Code layout so a future tool boundary (exposing this agent as an MCP server, or consuming external MCP servers as tools) has a home without reshaping the project. Build it when a tool actually needs to cross the boundary.
